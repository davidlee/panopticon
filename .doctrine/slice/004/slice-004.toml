id = 4
slug = "desktop-watcher-migration"
title = "Desktop watcher docs and operational migration"
status = "reconcile"
created = "2026-07-13"
updated = "2026-07-20"
tags = []

[relationships]
needs = ["SL-002"]                      # slice→work hard prerequisite
after = [{ to = "SL-003", rank = 0 }]                      # slice→work soft sequence: { to, rank }

[[selector]]
selector = "README.md"
intent = "design-target"

[[selector]]
selector = "docs/schema.md"
intent = "design-target"

[[selector]]
selector = "docs/privacy.md"
intent = "design-target"

[[selector]]
selector = "panopticon/store.py"
intent = "design-target"

[[selector]]
selector = "panopticon/desktop_watcher/__main__.py"
intent = "design-target"

[[selector]]
selector = "panopticon/segmentizer/__main__.py"
intent = "design-target"

[[selector]]
selector = "panopticon/segmentizer/retention.py"
intent = "design-target"

[[selector]]
selector = "tests/test_store.py"
intent = "design-target"

[[selector]]
selector = "tests/test_desktop_main.py"
intent = "design-target"

[[selector]]
selector = "tests/test_segmentizer_retention.py"
intent = "design-target"

[[selector]]
selector = "tests/test_segmentizer_main.py"
intent = "design-target"

[[relation]]
label = "references"
role = "implements"
target = "SPEC-001"

[[relation]]
label = "related"
target = "DEC-001"

# STRUCTURAL relations are uniform `[[relation]]` rows, never a typed
# `[relationships]` axis (SL-048 "the cut"). Slices carry no typed STRUCTURAL
# relation axes — the `[relationships]` table above holds only the dep/seq payload
# (`needs`/`after`, SL-060). Author a structural edge with the `doctrine link` verb
# — it validates the (source, label) pair and resolves the target:
#   doctrine link SL-NNN specs SPEC-018
#   doctrine link SL-NNN governed_by ADR-010
# `doctrine inspect SL-NNN` renders the inbound + outbound edges.
